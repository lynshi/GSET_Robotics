task main(){
     SetSensorUltrasonic(S2);
     TextOut(0,2,"Set distance");

     float usAvg = 0;
     
     for(int i = 0; i < 5; i++){
             usAvg += SENSOR_2;
     }

     usAvg /= 5.0;

     TextOut(0,2,NumToStr(usAvg));
     Wait(5000);
}
